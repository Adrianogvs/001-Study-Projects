-- COMANDOS SQL BASICOS

/*
 
SELECT DISTINCT {*, COLUNA [ALIAS], ...}
FROM TABLE
WHERE COLUNA;


  SELECT     : E uma lista de uma ou mais colunas.
  DISTINCT   : Suprime duplicidades
  *          : Seleciona todas as colunas
  colunm     : Seleciona a coluna nomeada
  alias      : Fornece para as colunas selecionadas titulos diferentes.
  FROM table : Especifica a tabela que contém as colunas
  WHERE      : Fazer uma busca em determinada coluna

 */


